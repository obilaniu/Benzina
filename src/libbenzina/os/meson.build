#
# OS-specific code.
#
# For certain supported combinations of OSes and architectures, specialized
# code is available; Otherwise, generics that can be overridden are provided.
#

subdir('generic')
if HOST_SYSTEM in ['linux']
  subdir(HOST_SYSTEM)
endif
